{Module Core/JSON
  {Import Core/ToJSON as To}
  {Import Core/FromJSON as From}
  {Export FromJSON ToJSON}
  {Rules
    {R FromJSON {FromJSON s_} {From/FromJSON s_} :guard IsStr({Frozen s_})}
    {R ToJSON {ToJSON x_} {To/ToJSON x_} :guard True}
  }
}