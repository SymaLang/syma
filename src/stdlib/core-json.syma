{Module Core/JSON
  {Import Core/ToJSON as To}
  {Import Core/FromJSON as From}
  {Export FromJSON ToJSON}
  {Rules
    {{FromJSON s_} -> {From/FromJSON s_} :guard IsStr({Frozen s_})}
    {{ToJSON x_} -> {To/ToJSON x_}}
  }
}