{Module Core/JSON
  {Import Core/ToJSON as To}
  {Import Core/FromJSON as From}
  {Export FromJSON ToJSON}
  {Rules
    {:rule ToJSON {ToJSON x_} -> {To/ToJSON x_}}
    {:rule FromJSON {FromJSON s_} -> {From/FromJSON s_} :guard IsStr(s_)}
  }
}