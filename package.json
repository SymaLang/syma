{
  "name": "syma-fe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "node scripts/sym-2-json.js src/todo.lisp --out public/universe.json --pretty",
    "watch": "nodemon --watch 'src/**/*.lisp' --ext lisp --exec 'npm run compile'"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.3.0",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "tailwindcss": "^4.1.13"
  }
}
